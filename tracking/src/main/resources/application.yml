# HTTP Server
server:  
  port: 1111   # HTTP port

spring:  
  application:
    name: tracking
    
endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true    